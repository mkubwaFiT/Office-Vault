# PyInstaller build artifacts
build/
dist/
*.spec.bak

# PyInstaller onedir runtime bundle (if built into / copied to the repo root)
_internal/
Vault_Toolkit/

# Compiled / cache
__pycache__/
*.py[cod]
*$py.class

# Built executables (produced by build_vault.py, not source)
*.exe

# Runtime vault data (user content / metadata, never commit)
TextVault_Data/
vault_metadata.json

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
